/// <reference types="vite/client" />

// Baked in at build time by vite.config.ts from version_autogen.h - the same
// version string the firmware itself reports (see version.h).
declare const __APP_VERSION__: string;
